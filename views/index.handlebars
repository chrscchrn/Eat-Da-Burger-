<h1 id="title">Burgers Burgers Burgers</h1>

<img src="https://media3.giphy.com/media/i4bWPx9KZWeMph3ZLv/source.gif" alt="Dancing Burger" class="inherit" width="200" height="200">

<p>Burgers that have been eaten</p>
  
<ol>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger devoured=true}}
    {{/unless}}
  {{/each}}
</ol> 

<p>Burgers that havent been eaten</p>

<ol>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger devoured=false}}
    {{/if}}
  {{/each}}
</ol>

<h2>Add a Burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>